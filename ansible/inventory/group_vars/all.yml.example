---
# Local configuration — copy to all.yml and fill in your values
# all.yml is gitignored — your secrets and IPs stay local
#
# cp all.yml.example all.yml

# Personal user account (used for bootstrap and cloud-init)
personal_user: myuser

# SSH public key (used in cloud-init for both personal and ansible users)
ssh_pubkey: "ssh-ed25519 AAAA... your-key-here"

# Password hash for personal user (generate with: openssl passwd -6)
password_hash: "$6$rounds=4096$salt$hash-here"

# Node IPs — must match your network/DHCP reservations
node_ip_1: 10.0.0.11
node_ip_2: 10.0.0.12
node_ip_3: 10.0.0.13
node_ip_4: 10.0.0.14

# K3s server endpoint (which node is control plane)
k3s_server_ip: "{{ node_ip_1 }}"
k3s_server_url: "https://{{ k3s_server_ip }}:6443"

# MetalLB IP range for LoadBalancer services (must not overlap with node/DHCP IPs)
metallb_ip_range: "10.0.0.100-10.0.0.110"

# Tailscale
tailscale_auth_key: "{{ lookup('file', playbook_dir + '/../../secrets/tailscale-auth-key') | trim }}"
tailscale_subnet_routes: "10.0.0.0/24"

# x86 K3s agent node IPs (Proxmox VMs)
x86_node_ip_1: 10.0.0.21
x86_node_ip_2: 10.0.0.22

# SFF node IPs
sff_athena_ip: 10.0.0.30
sff_artemis_ip: 10.0.0.31
sff_minisforum_ip: 10.0.0.32

# Wazuh manager IP
wazuh_manager_ip: 10.0.0.50

# Cloudflare tunnel domain
cloudflare_domain: example.com

# Local domain (for internal ingress, Gitea registry, etc.)
local_domain: yourdomain.local

# Gitea registry hostname (resolved via /etc/hosts on nodes)
gitea_registry_host: "gitea.{{ local_domain }}"
