---
# DEPRECATED — kept only as a fallback example.
#
# As of task #45 (cert-manager + internal CA), grafana / prometheus /
# alertmanager Ingresses are defined in k8s/ingress/local-ingress.yml.j2
# and rendered by 03-post-install.yml with TLS via the homielab-internal
# issuer.
#
# After rolling out the new templated ingress, delete any local
# `k8s/monitoring/ingress.local.yml` you have on disk to avoid drift:
#   rm k8s/monitoring/ingress.local.yml
