---
# DEPRECATED — kept only as a fallback example.
#
# As of task #45 (cert-manager + internal CA), chat.<local_domain> is defined
# in k8s/ingress/local-ingress.yml.j2 (Ingress name `open-webui-local`) and
# rendered by 03-post-install.yml.
#
# If you previously applied the standalone manifest below, the old Ingress
# was named `open-webui`. After rolling out the new templated ingress, delete
# the orphan:
#   kubectl -n open-webui delete ingress open-webui 2>/dev/null
#   rm k8s/open-webui/ingress.local.yml
