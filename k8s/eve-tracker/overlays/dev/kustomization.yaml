apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: eve-tracker-dev

resources:
  - ../../base
  - namespace.yml
  - secret.yml

images:
  - name: gitea.example.local/CHANGEME/eve-tracking-jobs-backend
    newTag: dev
  - name: gitea.example.local/CHANGEME/eve-tracking-jobs-frontend
    newTag: dev
