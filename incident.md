# Open incidents

_None._

When something fires, replace this file's contents with an entry following
[`docs/incidents/README.md`](docs/incidents/README.md)'s template. On
resolution, `git mv` it to `docs/incidents/YYYY-MM-DD-<slug>.md`, add a row
to the index table, and reset this file to the stub above.
